package com.vgp.recognizeartists.global;

import android.os.Environment;

public class Constants {
    public static final String IMG_DIR = Environment.getExternalStoragePublicDirectory(Environment.DIRECTORY_PICTURES) + "";
}
